<?php
defined('MOODLE_INTERNAL') || die();

global $CFG, $PAGE;

if ($hassiteconfig)
{
	/*
	$page = new admin_settingpage('notify', get_string('pluginname', 'local_notify'));
	
	$ADMIN->add('root', new admin_category('notify', get_string('pluginname', 'local_notify')));
	$ADMIN->add('notify', $page);
	
	*/
}
/*
if ($ADMIN->fulltree) {
	
	$settings->add(new admin_setting_heading('local_notify_birthday_header', get_string('birthday_header_settings', 'local_notify'), ''));
}
*/